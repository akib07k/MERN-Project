# MongoDB connection string (REQUIRED - MongoDB Atlas)
# Replace <db_password> with your actual password
# Replace 'dbname' with your database name (e.g., ecommerce)
MONGO_URI=mongodb+srv://akibk6005_db_user:<db_password>@cluster0.c1nhck4.mongodb.net/ecommerce?retryWrites=true&w=majority&appName=Cluster0

# Port (Render provides this automatically)
PORT=8080

# JWT Secret (REQUIRED - use a strong random string)
JWT_SECRET=your-secret-key-here

# Frontend URL for CORS (REQUIRED - your Render frontend URL)
FRONTEND_URL=https://your-frontend-name.onrender.com
