const API = import.meta.env.VITE_API_URL;

export const PRODUCTS_URL = `${API}/api/products`;
export const USERS_URL = `${API}/api/users`;
export const ORDERS_URL = `${API}/api/orders`;
export const UPLOAD_URL = `${API}/api/upload`;